package com.web.ddentist.hospital.site.bannerInfo.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.multipart.MultipartFile;

@RequestMapping("/hospital/site/bannerInfo")
@Controller
public class BannerInfoController {
	
	//요청URI : /hospital/site/banner
	@GetMapping("")
	public String main() {
		return "hospital/bannerInfo";
	}
	
	@GetMapping("/uploadFormAction")
	public String uploadFormAction(MultipartFile uploadFile) {
		
		
		
		return "redirect:/hospital/bannerInfo";
	}
	
}
